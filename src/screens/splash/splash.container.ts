import { connect } from 'react-redux';
import { SplashComponent } from './splash.view';

export const SplashContainer = connect(
  (state: any) => {
    return {
    }
  },
  (dispatch: any) => {
    return {
    }
  }
)(SplashComponent);
